# Allow installing packages with peer dependency mismatches.
# Needed because vite-plugin-electron@0.28 declares peer vite@^5 but works
# correctly at runtime with Vite 7. Remove once the plugin releases Vite 7 support.
legacy-peer-deps=true
